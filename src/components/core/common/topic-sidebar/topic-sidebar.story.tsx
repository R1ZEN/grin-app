import React from 'react';
import { TopicSidebar } from './topic-sidebar';

export default {
  title: 'components/core',
};

export const TopicSidebarDefault = () => {
  return (
    <TopicSidebar visible={false} />
  );
};